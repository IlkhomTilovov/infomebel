import { useHome } from "./home"
import { useClients } from "./clients"
import { useCompanies } from "./companies"
import { useCompaniesPage } from "./companiesPage"
export const useContexts = {
    useHome,
    useClients,
    useCompanies,
    useCompaniesPage
}